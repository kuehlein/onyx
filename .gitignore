# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
*.g.dart
*.freezed.dart

# Dev-only: app-written state files in the staged vault at runtime — the SRS
# snapshot and the readiness-target selection (real + dev-isolated variants).
# (The glossary note under _meta is authored content and stays tracked.)
staging/**/_meta/onyx-state*.json
staging/**/_meta/onyx-target*.json

# iOS
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec

# macOS
macos/Flutter/GeneratedPluginRegistrant.swift
macos/Flutter/ephemeral/

# Nix
result
.direnv/

# IDE
.idea/
*.iml
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Secrets — never commit
.env
*.key
